setting parameters during run in docker-compose:
docker-compose run -e MYENVVAR=actual_value my_service


for martin-github-runner 
export RUNNER_REPOSITORY_URL=https://github.com/martinFrank/martins-github-runner
export RUNNER_TOKEN="from github" 
sudo docker-compose -f "docker-compose.yml" up 


docker run -d -it --name my-github-runner -e RUNNER_NAME=my-runner -e GITHUB_ACCESS_TOKEN=token -e RUNNER_REPOSITORY_URL=https://github.com/... -v /var/run/docker.sock:/var/run/docker.sock tcardonne/github-runner
